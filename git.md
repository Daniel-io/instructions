
<type>(<scope>): <short description>

```bash
git commit -m "feat(game): add player movement logic"
git commit -m "fix(ui): prevent score counter from resetting incorrectly"
git commit -m "docs: update README with installation steps"
git commit -m "style(js): format code with Prettier"
git commit -m "refactor(controller): simplify input handling"
git commit -m "perf(render): optimize canvas redraw"
git commit -m "test(game): add unit test for collision detection"
git commit -m "chore: update dependencies"
git commit -m "ci: add GitHub Actions workflow for build"
```

